# ==========================================
# Tutor de Turma — API RAG (dependências de runtime)
# Embeddings e LLMs são consumidos via API da HuggingFace (nuvem),
# então NÃO é preciso torch/sentence-transformers localmente.
# ==========================================

# --- API / servidor HTTP ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # necessário para upload de arquivos (UploadFile)
pydantic>=2.7
python-dotenv>=1.0

# --- Leitura de documentos ---
pdfplumber>=0.11
python-docx>=1.1

# --- Pipeline RAG (LangChain) ---
langchain-classic>=1.0       # ConversationalRetrievalChain
langchain-core>=0.3
langchain-text-splitters>=0.3
langchain-huggingface>=0.1
langchain-mongodb>=0.2

# --- Banco vetorial (MongoDB Atlas) ---
pymongo>=4.6
dnspython>=2.6               # resolução do mongodb+srv://
