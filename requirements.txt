# ==========================================
# Chatbot IA (RAG) — dependências
# Compatível com Windows/Linux/macOS. No Windows, o pip baixa o
# torch CPU automaticamente (não há necessidade de CUDA).
# ==========================================

# --- API / servidor HTTP ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # necessário para upload de arquivos (UploadFile)
pydantic>=2.7
python-dotenv>=1.0

# --- Leitura de documentos ---
pdfplumber>=0.11
python-docx>=1.1

# --- Pipeline RAG (LangChain) ---
langchain-classic>=1.0       # ConversationalRetrievalChain
langchain-core>=0.3
langchain-text-splitters>=0.3
langchain-huggingface>=0.1
langchain-mongodb>=0.2

# --- Embeddings locais ---
sentence-transformers>=3.0   # modelo all-MiniLM-L6-v2 (puxa o torch)

# --- Banco vetorial ---
pymongo>=4.6

# --- Testes ---
pytest>=8.0
httpx>=0.27
